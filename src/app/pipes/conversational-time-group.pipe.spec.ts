import { ConversationalTimeGroupPipe } from './conversational-time-group.pipe';

describe('ConversationalTimeGroupPipe', () => {
  it('create an instance', () => {
    const pipe = new ConversationalTimeGroupPipe();
    expect(pipe).toBeTruthy();
  });
});
