import { ListSummaryPipe } from './list-summary.pipe';

describe('ListSummaryPipe', () => {
  it('create an instance', () => {
    const pipe = new ListSummaryPipe();
    expect(pipe).toBeTruthy();
  });
});
